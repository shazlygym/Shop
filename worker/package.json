{
  "name": "@swc/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "pretest": "DATABASE_URL=file:./test.db prisma db push --skip-generate --schema=../api/prisma/schema.prisma",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "@swc/shared": "*",
    "whatsapp-web.js": "^1.23.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "tsx": "^4.10.5",
    "vitest": "^1.6.0"
  }
}
